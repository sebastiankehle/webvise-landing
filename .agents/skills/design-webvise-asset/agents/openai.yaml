interface:
  display_name: "Design webvise Asset"
  short_description: "Design any asset in the webvise CI"
  default_prompt: "Use $design-webvise-asset to design a new webvise asset from the current CI and the closest approved reference."
